# Содержание 🌍 <a id="content"></a>

## [🧪 Основы Pytest](#основы-pytest)

- [Что такое Pytest?](#что-такое-pytest)
- [Как работает Pytest](#как-работает-pytest)
- [Поиск тестов и функций](#поиск-тестов-и-функций)

## [🔧 Фикстуры в Pytest](#фикстуры-в-pytest)

- [Что такое фикстуры?](#что-такое-фикстуры)
- [Создание фикстур](#создание-фикстур)
- [Жизненный цикл фикстур](#жизненный-цикл-фикстур)
- [Автоиспользование фикстур (autouse)](#автоиспользование-фикстур-autouse)
- [Финализаторы в фикстурах (yield)](#финализаторы-в-фикстурах-yield)

## [🎯 Параметризация тестов](#параметризация-тестов)

- [Параметризация через @pytest.mark.parametrize](#параметризация-через-pytestmarkparametrize)
- [Косвенная параметризация (indirect)](#косвенная-параметризация-indirect)
- [Комбинированная параметризация](#комбинированная-параметризация)

## [🚀 Параллельный запуск тестов](#параллельный-запуск-тестов)

- [Установка pytest-xdist](#установка-pytest-xdist)
- [Параллельный запуск тестов через -n](#параллельный-запуск-тестов-через-n)

## [🏷️ Маркеры в Pytest](#маркеры-в-pytest)

- [Что такое маркеры?](#что-такое-маркеры)
- [Стандартные маркеры: skip, skipif, xfail](#стандартные-маркеры-skip-skipif-xfail)
- [Создание пользовательских маркеров](#создание-пользовательских-маркеров)
- [Фильтрация тестов по маркерам](#фильтрация-тестов-по-маркерам)

## [🧩 Декораторы в Pytest](#декораторы-в-pytest)

- [Общая таблица декораторов](#общая-таблица-декараторов)
- [@pytest.mark.parametrize](#pytestmarkparametrize)
- [@pytest.mark.skip и skipif](#pytestmarkskip-и-skipif)
- [@pytest.mark.xfail](#pytestmarkxfail)

## [🔌 Плагины и расширения Pytest](#плагины-и-расширения-pytest)

- [Установка плагинов](#установка-плагинов)
- [Полезные плагины (xdist, cov, html)](#полезные-плагины-xdist-cov-html)
- [Примеры команд с плагинами](#примеры-команд-с-плагинами)

## [✅ Ассерты в Pytest](#ассерты-в-pytest)

- [Проверки через assert](#проверки-через-assert)
- [Проверка исключений через pytest.raises](#проверка-исключений-через-pytestraises)

## [⚙️ Конфигурация проекта](#конфигурация-проекта)

- [Файл pytest.ini](#файл-pytestini)
- [Настройки проекта через pytest.ini](#настройки-проекта-через-pytestini)
- [Структура тестового проекта](#структура-тестового-проекта)

## [🌟 Рекомендации по написанию тестов](#рекомендации-по-написанию-тестов)

- [Лучшие практики при работе с Pytest](#лучшие-практики-при-работе-с-pytest)
- [Антипаттерны в тестах](#антипаттерны-в-тестах)

---

## 🧪 Основы Pytest <a id="основы-pytest"></a>

### Что такое Pytest? <a id="что-такое-pytest"></a>

**Pytest** — это мощный и удобный фреймворк для тестирования на Python.
Он используется для написания юнит-, интеграционных и функциональных тестов.
Pytest минимизирует шаблонный код, имеет поддержку фикстур, параметризации, плагинов и легко интегрируется в процессы CI/CD.

[🔼 К содержанию](#content)

---

### Как работает Pytest <a id="как-работает-pytest"></a>

Pytest проходит несколько стадий работы:

- Ищет файлы и функции по определённым шаблонам имен.
- Собирает тесты в дерево выполнения, применяя необходимые фикстуры и параметры.
- Запускает тесты в определённой последовательности.
- Отображает детальный отчёт о результате выполнения каждого теста, указывая успешные, проваленные и пропущенные тесты.

[🔼 К содержанию](#content)

---

### Поиск тестов и функций <a id="поиск-тестов-и-функций"></a>

Pytest автоматически находит тесты по следующим правилам:

- Файлы должны начинаться с `test_` или заканчиваться на `_test.py`.
- Функции должны начинаться с `test_`.
- Классы должны начинаться с `Test` и не иметь конструкторов `__init__`.

Эти соглашения позволяют Pytest автоматически определить, что именно является тестом для выполнения.

[🔼 К содержанию](#content)

---

## 🔧 Фикстуры в Pytest <a id="фикстуры-в-pytest"></a>

### Что такое фикстуры? <a id="что-такое-фикстуры"></a>

Фикстуры в Pytest — это специальные функции для подготовки и очистки тестового окружения.
Они позволяют избежать дублирования кода, упростить настройку тестов и сделать их более читаемыми и модульными.

[🔼 К содержанию](#content)

---

### Создание фикстур <a id="создание-фикстур"></a>

Фикстура создаётся как обычная функция, которую Pytest использует для передачи подготовленных данных или состояния в тесты.
Фикстуры могут зависеть друг от друга, поддерживают параметризацию и позволяют контролировать создание и уничтожение ресурсов.

[🔼 К содержанию](#content)

---

### Жизненный цикл фикстур <a id="жизненный-цикл-фикстур"></a>

Жизненный цикл фикстуры определяется её областью видимости (scope).
Фикстура может создаваться и уничтожаться один раз за тест, за класс, за модуль или за всю тестовую сессию.
Выбор области видимости помогает оптимизировать скорость тестирования и управление ресурсами.

[🔼 К содержанию](#content)

---

### Автоиспользование фикстур (autouse) <a id="автоиспользование-фикстур-autouse"></a>

Фикстуры можно подключать автоматически ко всем тестам без явного указания их в аргументах.
Для этого используется параметр `autouse`.
Такой подход удобен для выполнения обязательных действий вроде настройки окружения или логирования.

[🔼 К содержанию](#content)

---

### Финализаторы в фикстурах (yield) <a id="финализаторы-в-фикстурах-yield"></a>

Финализаторы позволяют выполнять код после завершения теста для освобождения ресурсов.
Фикстура разделяется на две части: подготовку данных перед тестом и очистку после теста с помощью конструкции `yield`.

[🔼 К содержанию](#content)

---

## 🎯 Параметризация тестов <a id="параметризация-тестов"></a>

### Параметризация через @pytest.mark.parametrize <a id="параметризация-через-pytestmarkparametrize"></a>

Параметризация в Pytest позволяет запускать один и тот же тест с разными входными данными. Это помогает избежать дублирования кода и упростить покрытие различных сценариев. Основной способ параметризации — через декоратор `@pytest.mark.parametrize`. Он принимает два аргумента: строку с именами параметров и список значений. Для каждой комбинации значений Pytest запускает отдельный тест. Это удобно при проверке одной логики на множестве входов. Количество прогонов теста зависит от количества элементов в списке.

[🔼 К содержанию](#content)

---

### Косвенная параметризация (indirect)<a id="косвенная-параметризация-indirect"></a>

Косвенная параметризация применяется, когда передаваемые значения должны быть обработаны фикстурами. В этом случае Pytest сначала передаёт значение в фикстуру, а уже результат её выполнения — в тестовую функцию. Чтобы включить косвенную параметризацию, используется параметр `indirect=True`. Это полезно, если нужно создать ресурс, подготовить данные или провести предварительную настройку перед тестом. Такой подход помогает сохранить чистоту и читаемость кода, особенно при работе с внешними ресурсами или подготовкой окружения.

[🔼 К содержанию](#content)

---

### Комбинированная параметризация<a id="комбинированная-параметризация"></a>

Комбинированная параметризация используется, когда необходимо протестировать множество комбинаций параметров. Для этого можно применить несколько декораторов `@pytest.mark.parametrize` над одной функцией. В результате будет создано декартово произведение всех значений, и тест выполнится для каждой возможной пары. Это позволяет выявить проблемы, возникающие при различных сочетаниях входных данных. Комбинированная параметризация особенно полезна при тестировании функций с несколькими аргументами, зависящими друг от друга.

[🔼 К содержанию](#content)

---

## 🚀 Параллельный запуск тестов <a id="параллельный-запуск-тестов"></a>

### Установка pytest-xdist <a id="установка-pytest-xdist"></a>

Для параллельного запуска тестов в Pytest используется плагин `pytest-xdist`. Он позволяет распределить выполнение тестов по нескольким ядрам процессора, что значительно ускоряет общее время прогона. Установка осуществляется через pip командой `pip install pytest-xdist`. После установки можно использовать дополнительные параметры при запуске тестов, такие как `-n` для указания количества параллельных потоков. Этот инструмент особенно полезен в больших проектах с большим числом автотестов.

[🔼 К содержанию](#content)

---

### Параллельный запуск тестов через -n <a id="параллельный-запуск-тестов-через-n"></a>

Параллельный запуск тестов осуществляется с помощью флага `-n`, за которым следует число параллельных процессов. Например, команда `pytest -n 4` запустит тесты в четыре потока. Pytest автоматически распределяет тестовые функции между доступными процессами. Это может значительно сократить общее время выполнения, особенно при большом количестве независимых тестов. Однако при использовании параллельного запуска важно учитывать возможные конфликты при доступе к общим ресурсам или файлам, чтобы избежать нестабильного поведения тестов.

[🔼 К содержанию](#content)

---

## 🏷️ Маркеры в Pytest <a id="маркеры-в-pytest"></a>

### Что такое маркеры? <a id="что-такое-маркеры"></a>

Маркеры в Pytest — это способ пометить тесты определёнными тегами для группировки, фильтрации или изменения поведения. С помощью маркеров можно запускать только определённые категории тестов, временно отключать их или указать ожидаемый результат. Pytest предоставляет как встроенные маркеры, так и возможность создавать собственные. Маркеры повышают гибкость тестирования и позволяют лучше управлять большим числом тестов в проекте.

| Маркер                        | Описание                                                                        |
| ----------------------------- | ------------------------------------------------------------------------------- |
| `@pytest.mark.skip`           | Пропускает тест без выполнения                                                  |
| `@pytest.mark.skipif`         | Пропускает тест, если условие истинно (например, `sys.version_info < (3, 8)`)   |
| `@pytest.mark.xfail`          | Ожидает, что тест упадёт (если проходит — отмечает как неожиданный успех)       |
| `@pytest.mark.parametrize`    | Запускает тест с разными параметрами (параметризация)                           |
| `@pytest.mark.usefixtures`    | Автоматически использует указанные фикстуры для теста                           |
| `@pytest.mark.filterwarnings` | Фильтрует предупреждения в тесте                                                |
| `@pytest.mark.timeout`        | Устанавливает таймаут для теста (например, `@pytest.mark.timeout(10)`)          |
| `@pytest.mark.asyncio`        | Помечает тест как асинхронный (требуется `pytest-asyncio`)                      |
| `@pytest.mark.slow`           | Пользовательский маркер для медленных тестов (нужна регистрация в `pytest.ini`) |
| `@pytest.mark.smoke`          | Пользовательский маркер для "дымовых" тестов                                    |
| `@pytest.mark.order`          | Задаёт порядок выполнения тестов (требуется `pytest-ordering`)                  |
| `@pytest.mark.dependency`     | Указывает зависимости между тестами (требуется `pytest-dependency`)             |

[🔼 К содержанию](#content)

---

### Стандартные маркеры: skip, skipif, xfail <a id="стандартные-маркеры-skip-skipif-xfail"></a>

Pytest предоставляет несколько встроенных маркеров, среди которых наиболее часто используемые — это `skip`, `skipif` и `xfail`.
- `@pytest.mark.skip` полностью пропускает выполнение теста.
- `@pytest.mark.skipif(condition, reason=...)` пропускает тест при выполнении определённого условия.
- `@pytest.mark.xfail` помечает тест как ожидаемо падающий, то есть его провал не влияет на итог выполнения всех тестов.

Эти маркеры помогают гибко управлять тестами в зависимости от контекста, окружения или стадии разработки.

[🔼 К содержанию](#content)

---

### Создание пользовательских маркеров <a id="создание-пользовательских-маркеров"></a>

Пользовательские маркеры позволяют организовать тесты по категориям, например, `@pytest.mark.ui`, `@pytest.mark.api`, `@pytest.mark.regression` и т. д. Такие маркеры помогают фильтровать тесты при запуске. Чтобы избежать предупреждений, все пользовательские маркеры должны быть зарегистрированы в файле `pytest.ini` с помощью секции `markers`. Это делает проект более структурированным и упрощает запуск определённых наборов тестов.

[🔼 К содержанию](#content)

---

### Фильтрация тестов по маркерам <a id="фильтрация-тестов-по-маркерам"></a>

Для запуска тестов с определённым маркером используется опция `-m` в командной строке. Например, команда `pytest -m "ui"` запустит только те тесты, которые помечены маркером `@pytest.mark.ui`. Также можно комбинировать условия, например `-m "api and not slow"`. Фильтрация по маркерам особенно полезна для запуска тестов по категориям, при раздельном тестировании компонентов или в CI/CD пайплайнах.

 **Простая фильтрация по одному маркеру**
```python
import pytest

@pytest.mark.ui
def test_login_page():
    assert True

@pytest.mark.api
def test_get_users():
    assert True

# Запуск только UI-тестов: pytest -m "ui"
# Запуск только API-тестов: pytest -m "api"
```

 **Комбинированная фильтрация (`and`, `or`, `not`)**
```python
import pytest

@pytest.mark.ui
@pytest.mark.smoke
def test_homepage_load():
    assert True

@pytest.mark.api
@pytest.mark.slow
def test_large_data_fetch():
    assert True

# Запуск smoke-тестов в UI: pytest -m "ui and smoke"
# Запуск НЕ медленных API-тестов: pytest -m "api and not slow"
# Запуск всех smoke ИЛИ slow тестов: pytest -m "smoke or slow"
```

[🔼 К содержанию](#content)

---

## 🧩 Декораторы в Pytest <a id="декораторы-в-pytest"></a>

### Общая таблица декораторов <a id="общая-таблица-декараторов"></a>

В Pytest декораторы служат для изменения поведения тестов, параметризации и управления их выполнением. Основные декораторы включают параметризацию тестов, пропуск или ожидаемый провал, а также маркировку тестов для фильтрации. Каждый декоратор выполняет свою роль и помогает гибко настраивать процесс тестирования. Использование декораторов делает код тестов компактным и удобным для поддержки.

| Декоратор                  | Описание                                                                                       | Пример использования                                                                       |
|----------------------------|-----------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------|
| `@pytest.mark.parametrize` | Параметризация тестов для проверки функции с разными данными.                                  | `@pytest.mark.parametrize("input,expected", [(1,2), (2,3)])`                             |
| `@pytest.mark.skip`        | Пропуск теста с указанием причины.                                                            | `@pytest.mark.skip(reason="В разработке")`                                               |
| `@pytest.mark.skipif`      | Пропуск теста при выполнении определенного условия.                                           | `@pytest.mark.skipif(sys.platform == "win32", reason="Не поддерживается на Windows")`     |
| `@pytest.mark.xfail`       | Отметить тест как ожидаемо падающий (например, если баг еще не исправлен).                    | `@pytest.mark.xfail(reason="Известный баг")`                                             |
| `@pytest.fixture`          | Создание фикстуры для подготовки данных или окружения.                                        | `@pytest.fixture\ndef setup_data(): return {"key": "value"}`                              |
| `@pytest.mark.usefixtures` | Указание использования одной или нескольких фикстур в тесте.                                  | `@pytest.mark.usefixtures("setup_data")`                                                 |
| `@pytest.mark.timeout`     | Установить ограничение времени выполнения для теста (используется с плагином `pytest-timeout`). | `@pytest.mark.timeout(5)`                                                                |
| `@pytest.mark.asyncio`     | Для тестирования асинхронного кода с использованием `async` функций.                          | `@pytest.mark.asyncio\ndef test_async(): await some_async_function()`                    |

[🔼 К содержанию](#content)

---

### @pytest.mark.parametrize <a id="pytestmarkparametrize"></a>

Декоратор `@pytest.mark.parametrize` позволяет запускать один тест с разными наборами входных данных. Он принимает имена параметров и список значений, создавая отдельный прогон теста для каждого набора. Это значительно сокращает дублирование кода и улучшает покрытие тестами различных вариантов. Параметризация — одна из самых мощных возможностей Pytest для эффективного тестирования.

[🔼 К содержанию](#content)

---

### @pytest.mark.skip и skipif <a id="pytestmarkskip-и-skipif"></a>

Декораторы `@pytest.mark.skip` и `@pytest.mark.skipif` позволяют пропускать тесты. Первый полностью отключает выполнение теста без условия, второй — пропускает тест при выполнении заданного условия. Они полезны для временного исключения тестов, которые могут быть нестабильными или неактуальными в определённых окружениях, сохраняя чистоту отчётов и предотвращая ложные ошибки.

[🔼 К содержанию](#content)

---

### @pytest.mark.xfail <a id="pytestmarkxfail"></a>

Декоратор `@pytest.mark.xfail` помечает тест как ожидаемо падающий. Это значит, что если тест падает, итог выполнения не будет считаться ошибкой. Если же такой тест неожиданно проходит, Pytest отметит это как "XPASS". Этот декоратор полезен при отслеживании известных багов и в процессе разработки, когда часть функционала ещё не реализована или нестабильна.

[🔼 К содержанию](#content)

---

## 🔌 Плагины и расширения Pytest <a id="плагины-и-расширения-pytest"></a>

### Установка плагинов <a id="установка-плагинов"></a>

Плагины в Pytest расширяют функциональность фреймворка и позволяют автоматизировать дополнительные задачи, такие как параллельный запуск, сбор покрытия кода или генерация отчётов. Установка плагинов происходит стандартным способом через `pip install`. После установки плагины автоматически интегрируются с Pytest и доступны при запуске тестов. Иногда требуется дополнительная настройка через конфигурационные файлы.

[🔼 К содержанию](#content)

---

### Полезные плагины (xdist, cov, html) <a id="полезные-плагины-xdist-cov-html"></a>

Среди самых популярных плагинов для Pytest —
- `pytest-xdist` — для параллельного запуска тестов и распределения нагрузки по ядрам процессора,
- `pytest-cov` — для измерения покрытия кода тестами и создания отчётов,
- `pytest-html` — для генерации красивых HTML-отчётов с результатами тестов.

Эти плагины значительно повышают удобство и эффективность тестирования, а также помогают анализировать качество кода.

[🔼 К содержанию](#content)

---

### Примеры команд с плагинами <a id="примеры-команд-с-плагинами"></a>

Команды запуска тестов с использованием популярных плагинов:
- `pytest -n 4` — параллельный запуск тестов в 4 потока (pytest-xdist),
- `pytest --cov=src` — запуск с измерением покрытия кода в папке `src` (pytest-cov),
- `pytest --html=report.html` — генерация HTML-отчёта с результатами (pytest-html),
- `pytest -n 2 --cov=src --html=report.html` — комбинированный запуск с параллельностью, покрытием и отчётом.

Использование плагинов позволяет гибко настраивать процесс тестирования под нужды проекта.

[🔼 К содержанию](#content)

---

## ✅ Ассерты в Pytest <a id="ассерты-в-pytest"></a>

### Проверки через assert <a id="проверки-через-assert"></a>

В Pytest для проверки условий используется встроенное ключевое слово `assert`. Это простой и удобный способ сравнивать значения, проверять логические выражения и гарантировать правильность работы кода. При провале проверки Pytest автоматически выводит подробную информацию о выражении и значениях переменных, что облегчает отладку. Такой подход исключает необходимость использовать отдельные методы или классы для ассертов, как в некоторых других тестовых фреймворках.

**Простые проверки**
```python
def test_addition():
    result = 2 + 2
    assert result == 4  # Проверка равенства

def test_boolean():
    is_valid = True
    assert is_valid  # Проверка на True

def test_contains():
    text = "Hello, pytest!"
    assert "pytest" in text  # Проверка вхождения подстроки
```
**Проверки с сообщением об ошибке**
```python
def test_division():
    a, b = 10, 2
    assert b != 0, "Деление на ноль!"  # Сообщение при провале
    assert a / b == 5
```
**Проверки коллекций**
```python
def test_list_contents():
    items = [1, 2, 3]
    assert len(items) == 3
    assert 2 in items
    assert [] == []  # Сравнение пустых списков

def test_dict_keys():
    data = {"id": 1, "name": "Alice"}
    assert "name" in data
    assert data.get("age") is None  # Проверка отсутствия ключа
```
[🔼 К содержанию](#content)

---

### Проверка исключений через pytest.raises <a id="проверка-исключений-через-pytestraises"></a>

Для проверки, что определённый код вызывает ожидаемое исключение, в Pytest используется контекстный менеджер `pytest.raises`. Внутри блока `with pytest.raises(Исключение):` размещается код, который должен привести к ошибке. Если исключение не возникло, тест считается проваленным. Этот способ позволяет точно контролировать появление ошибок и тестировать обработку исключительных ситуаций в коде.

[🔼 К содержанию](#content)

---

## ⚙️ Конфигурация проекта <a id="конфигурация-проекта"></a>

### Файл pytest.ini <a id="файл-pytestini"></a>

Файл `pytest.ini` — это основной конфигурационный файл для Pytest. В нём можно задавать общие настройки тестового фреймворка, такие как директории для поиска тестов, регистрация пользовательских маркеров, параметры запуска и отчётности. Наличие `pytest.ini` в корне проекта позволяет централизованно управлять поведением тестов и упрощает интеграцию с CI/CD системами.

[🔼 К содержанию](#content)

---

### Настройки проекта через pytest.ini <a id="настройки-проекта-через-pytestini"></a>

В файле `pytest.ini` можно определить секции и опции, например:
- `testpaths` — список папок с тестами,
- `markers` — регистрация кастомных маркеров,
- `addopts` — дополнительные параметры запуска (например, для генерации отчётов),
- настройки логирования и др.

Это позволяет настроить поведение Pytest под нужды конкретного проекта без необходимости менять команды запуска.

[🔼 К содержанию](#content)

---

### Структура тестового проекта <a id="структура-тестового-проекта"></a>

Рекомендуемая структура тестового проекта обычно включает отдельную папку для тестов, например `tests/`, внутри которой размещаются файлы и модули с тестами. В корне лежит конфигурационный файл `pytest.ini`. Такой подход помогает поддерживать порядок, быстро находить нужные тесты и облегчает интеграцию с инструментами автоматизации и CI. Внутри папки тесты часто группируют по функциональным или техническим признакам.

[🔼 К содержанию](#content)

---

## 🌟 Рекомендации по написанию тестов <a id="рекомендации-по-написанию-тестов"></a>

### Лучшие практики при работе с Pytest <a id="лучшие-практики-при-работе-с-pytest"></a>

При написании тестов на Pytest рекомендуется придерживаться следующих практик: использовать параметризацию для сокращения дублирования кода, применять фикстуры для настройки окружения и подготовки данных, писать независимые и изолированные тесты, обеспечивать читаемость и простоту тестов, а также использовать маркеры для организации и фильтрации тестов. Следование этим принципам помогает создавать поддерживаемый и масштабируемый набор автотестов.

[🔼 К содержанию](#content)

---

### Антипаттерны в тестах <a id="антипаттерны-в-тестах"></a>

К распространённым антипаттернам в тестах относятся: дублирование кода вместо параметризации, чрезмерная зависимость тестов друг от друга, сложные и запутанные тесты, отсутствие использования фикстур для подготовки окружения, игнорирование обработки исключений и слабая читаемость. Такие практики усложняют поддержку тестового набора и снижают эффективность автоматизации. Важно избегать их для стабильной и качественной работы с тестами.

[🔼 К содержанию](#content)

---






















































