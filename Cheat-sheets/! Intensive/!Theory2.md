## Содержание 🌍 <a id="content"></a>

[Дайте определение понятия «автоматизированное тестирование»](#автоматизированное-тестирование)  
[Основные виды автоматизированного тестирования](#виды-автоматизированного-тестирования)  
[Какие основные преимущества автоматизированного тестирования?](#преимущества-автоматизированного-тестирования)  
[Какие тесты автоматизировать, какие нет?](#какие-тесты-автоматизировать)  
[К каким проектам применима и к каким не применима автоматизация](#применимость-автоматизации)  

[Что такое GIT?](#что-такое-git)  
[Команды GIT](#команды-git)  

[TestNG](#testng)  
- [Обзор](#testng-обзор)  
- [Настройка теста](#testng-настройка-теста)  
- [Игнорирование тестов](#testng-игнорирование-тестов)  
- [Совместное выполнение тестов](#testng-совместное-выполнение-тестов)  
- [Тестирование исключений](#testng-тестирование-исключений)  
- [Параметризованные тесты](#testng-параметризованные-тесты)  
- [Тайм-аут теста](#testng-тайм-аут-теста)  
- [Зависимые тесты](#testng-зависимые-тесты)  
- [Порядок выполнения тестов](#testng-порядок-выполнения-тестов)  
- [Пользовательские имена тестов](#testng-пользовательские-имена-тестов)  

[JUnit](#junit)  
- [Обзор](#junit-обзор)  
- [Настройка теста](#junit-настройка-теста)  
- [Игнорирование тестов](#junit-игнорирование-тестов)  
- [Совместное выполнение тестов](#junit-совместное-выполнение-тестов)  
- [Тестирование исключений](#junit-тестирование-исключений)  
- [Параметризованные тесты](#junit-параметризованные-тесты)  
- [Тайм-аут теста](#junit-тайм-аут-теста)  
- [Зависимые тесты](#junit-зависимые-тесты)  
- [Порядок выполнения тестов](#junit-порядок-выполнения-тестов)  
- [Пользовательские имена тестов](#junit-пользовательские-имена-тестов)  

[CI/CD](#ci-cd)  
- [Что такое CI/CD?](#что-такое-ci-cd)  
- [В чем разница между Continuous Delivery и Continuous Deployment?](#разница-continuous-delivery-continuous-deployment)  
- [На больших проектах, реально ли автоматически всё происходит?](#ci-cd-на-больших-проектах)  
- [CI/CD цикл](#ci-cd-цикл)  
- [Практический опыт с любым CI/CD инструментом (GitLab, Jenkins, GitHub Actions)](#ci-cd-инструменты)  

[Docker (базовые знания)](#docker-базовые-знания)  
[Инструменты (фреймворки) автоматизации](#инструменты-автоматизации)  
[В чем отличие виртуализации и контейнеризации?](#виртуализация-контейнеризация)  

[Инструменты CI](#инструменты-ci)  

[Основные инструменты тестирования (для Java)](#инструменты-тестирования-java)  
- [Разница между JUnit и TestNG](#разница-junit-testng)  
- [Самые популярные фреймворки TestNG и JUnit](#популярные-фреймворки-testng-junit)  
- [Основные аннотации](#основные-аннотации)  

### Автоматизированное тестирование <a id="автоматизированное-тестирование"></a>

Автоматизированное тестирование — это проверка программного обеспечения с использованием специализированных инструментов, включающая проведение таких основных функций и шагов теста: запуск, инициализация, выполнение, анализ и выдача результата.

[К содержанию](#content)

---

### Основные виды автоматизированного тестирования <a id="основные-виды-автоматизированного-тестирования"></a>

1. **Автоматизация тестирования кода (Code-driven testing)**  
   Тестирование на уровне программных модулей, классов и библиотек (фактически, автоматические юнит-тесты).

2. **Автоматизация тестирования графического пользовательского интерфейса (Graphical user interface testing)**  
   Специальная программа (фреймворк автоматизации тестирования) позволяет генерировать пользовательские события — нажатия клавиш, клики мышкой, и отслеживать реакцию программы на эти действия, соответствующую спецификации.

3. **Автоматизация тестирования API (Application Programming Interface)**  
   Тестируются интерфейсы, предназначенные для взаимодействия, например, с другими программами или с пользователем. Как правило, используются специальные фреймворки.

[К содержанию](#content)

---

### Преимущества автоматизированного тестирования <a id="преимущества-автоматизированного-тестирования"></a>

- Обеспечение максимального покрытия тестами при регрессионном тестировании
- Быстрое выполнение
- Повторяемость тест кейсов
- Сокращение затрат на техническое обслуживание программного обеспечения
- Экономия времени тестировщиков
- Возможность исполнения в нерабочее время
- Исключение «человеческого» фактора
- Автоматически рассылаемые и сохраняемые отчеты о результатах тестирования

[К содержанию](#content)

---

### Какие тесты автоматизировать, какие нет? <a id="какие-тесты-автоматизировать"></a>

Автоматизируем те тесты, которые мы регулярно проходим — это smoke и регрессию. Регрессию проходим регулярно, сортируем по принципу, какие тесты можно автоматизировать быстрее всего.

[К содержанию](#content)

---

### Применимость автоматизации <a id="применимость-автоматизации"></a>

Автоматизировать можно всё, но вопрос в целесообразности.

[К содержанию](#content)

### GIT — Система контроля версий <a id="git-система-контроля-версий"></a>

GIT — это система контроля версий, которая позволяет хранить всю историю изменений, внесённых в проект. Это удобно, поскольку вы можете откатиться на предыдущие версии проекта.

[К содержанию](#content)

---

### Команды Git <a id="команды-git"></a>

#### git pull <a id="git-pull"></a>  
Если у вас есть ветка, настроенная на отслеживание удалённой ветки, то вы можете использовать команду `git pull`. Она автоматически извлекает и затем сливает данные из удалённой ветки в вашу текущую ветку. Выполнение `git pull` обычно извлекает (fetch) данные с сервера, с которого вы изначально склонировали, и автоматически пытается слить (merge) их с кодом, над которым вы в данный момент работаете.

#### git push <a id="git-push"></a>  
Когда вы хотите поделиться своими наработками, отправьте их в главный репозиторий с помощью команды:  
`git push [удал. сервер] [ветка]`.

#### git commit <a id="git-commit"></a>  
Для того чтобы записать изменения в репозиторий, используйте команду `git commit`.

#### git branch <a id="git-branch"></a>  
Для просмотра веток используйте команду `git branch`.  
Для создания новой ветки под названием `testing` используйте команду:  
`git branch testing`.  
Чтобы удалить ветку, используйте команду:  
`git branch -d`.

#### git rebase / git merge <a id="git-rebase-merge"></a>  
Для перемещения изменений из одной ветки в другую, используйте две команды:  
- `git merge` — слияние веток. Перейдите на ту ветку, в которую вы хотите слить изменения, и выполните команду:  
  `git checkout master`  
  `git merge otherVetka`  
- `git rebase` — перемещение веток.

#### git config <a id="git-config"></a>  
Для изменения настроек Git используйте команду `git config`. Пример:  
`git config --global user.name "John Doe"`  
`git config --global user.email johndoe@example.com`

#### git cherry-pick <a id="git-cherry-pick"></a>  
Команда `git cherry-pick` применяется для фиксации изменений, представленных коммитом на текущей ветке, не изменяя существующую историю.

#### git stash <a id="git-stash"></a>  
Для сохранения состояния проекта используйте команды:  
- `git stash` — сохраняет текущее состояние.  
- `git stash list` — просмотр списка существующих сохранённых состояний.  
- `git stash apply` — восстанавливает последний stash.  
- `git stash pop` — восстанавливает последний и удаляет его.  
- `git stash drop stash@{2}` — удаляет нужный по индексу stash.

#### git revert / git reset <a id="git-revert-reset"></a>  
Команда `git revert` безопасно отменяет операцию без потери истории коммитов, создавая новый коммит с отменёнными изменениями. Пример использования с commit-хэшами:  
`git revert [SHA]`.  
Для отката с помощью диапазонов:  
`git revert HEAD~[num-of-commits-back]`.

Команда `git reset` перемещает указатель HEAD и ветки на заданный коммит, изменяя состояние деревьев. Аргументы `--soft`, `--mixed` и `--hard` определяют, как изменяются проиндексированные файлы и рабочий каталог.

[К содержанию](#content)

---

### TestNG <a id="testng"></a>

#### 1. Обзор <a id="обзор"></a>  
TestNG работает для функционального тестирования и более высоких уровней тестирования.

[К содержанию](#content)

---

#### 2. Настройка теста <a id="настройка-теста"></a>  
Подобно JUnit, TestNG обеспечивает инициализацию и очистку на уровне метода и класса. На уровне класса используются аннотации `@BeforeClass` и `@AfterClass`, а на уровне метода — `@BeforeMethod` и `@AfterMethod`.

TestNG также предлагает дополнительные аннотации для конфигураций на разных уровнях:
- `@BeforeSuite`, `@AfterSuite`, `@BeforeGroup`, `@AfterGroup` — для конфигураций на уровне набора (suite) и группы.
- `@BeforeTest` и `@AfterTest` — для конфигурации до или после тест-кейсов, включенных в тег в XML-файле конфигурации TestNG.

[К содержанию](#content)

---

#### 3. Игнорирование тестов <a id="игнорирование-тестов"></a>  
В TestNG игнорировать тесты можно с помощью параметра `enabled` в аннотации `@Test`, установив его в значение `true` или `false`.

[К содержанию](#content)

---

#### 4. Совместное выполнение тестов <a id="совместное-выполнение-тестов"></a>  
В TestNG возможно совместное выполнение тестов в виде коллекции, что можно организовать с помощью файла XML. Например, `RegistrationTest` и `SignInTest` могут работать вместе.

Помимо группировки классов, TestNG также может группировать методы с помощью аннотации `@Test(groups = "groupName")`.

[К содержанию](#content)

---

#### 5. Тестирование исключений <a id="тестирование-исключений"></a>  
В TestNG можно реализовать тестирование исключений аналогично другим фреймворкам.

[К содержанию](#content)

---

#### 6. Параметризованные тесты <a id="параметризованные-тесты"></a>  
TestNG позволяет параметризовать тесты с помощью аннотаций `@Parameter` или `@DataProvider`.

[К содержанию](#content)

---

#### 7. Тайм-аут теста <a id="тайм-аут-теста"></a>  
TestNG позволяет задать тайм-аут для теста с помощью аннотации `@Test(timeout = 1000)`.

[К содержанию](#content)

---

#### 8. Зависимые тесты <a id="зависимые-тесты"></a>  
TestNG поддерживает зависимые тесты, что означает, что если начальный тест не пройден, все последующие зависимые тесты будут пропущены.

[К содержанию](#content)

---

#### 9. Порядок выполнения тестов <a id="порядок-выполнения-тестов"></a>  
TestNG не имеет неявного порядка выполнения методов тестирования. Однако можно управлять порядком выполнения, задав параметр `priority` в аннотации `@Test`.

[К содержанию](#content)

---

#### 10. Пользовательские имена тестов <a id="пользовательские-имена-тестов"></a>  
В настоящее время TestNG не предоставляет возможности задавать пользовательские имена тестов.

[К содержанию](#content)

---

### JUnit <a id="junit"></a>

#### 1. Обзор <a id="обзор"></a>  
JUnit послужил вдохновением для создания TestNG и широко используется для юнит-тестирования.

[К содержанию](#content)

---

#### 2. Настройка теста <a id="настройка-теста"></a>  
JUnit предлагает инициализацию и очистку на двух уровнях: до и после каждого метода и класса.  
Аннотации `@BeforeEach` и `@AfterEach` используются для метода, а `@BeforeAll` и `@AfterAll` — для класса.  
Методы `@BeforeClass` и `@AfterClass` должны быть статическими, что отличается от TestNG, где такие ограничения отсутствуют.

[К содержанию](#content)

---

#### 3. Игнорирование тестов <a id="игнорирование-тестов"></a>  
JUnit поддерживает игнорирование тестов с помощью аннотации `@Ignore`.

[К содержанию](#content)

---

#### 4. Совместное выполнение тестов <a id="совместное-выполнение-тестов"></a>  
В JUnit 5 мы можем использовать аннотации `@RunWith`, `@SelectPackages` и `@SelectClasses` для группировки тест-кейсов и выполнения их как набора (suite).  
Для запуска тестов из разных пакетов используется аннотация `@SelectPackages`.

[К содержанию](#content)

---

#### 5. Тестирование исключений <a id="тестирование-исключений"></a>  
В JUnit 5 для тестирования исключений используется метод `assertThrows`.  
В JUnit 4 можно использовать аннотацию `@Test(expected = DivideByZeroException.class)`.

[К содержанию](#content)

---

#### 6. Параметризованные тесты <a id="параметризованные-тесты"></a>  
JUnit поддерживает параметризованные тесты через аннотации:
- `@ParameterizedTest` — основная аннотация.
- `@ValueSource` — передает массив значений типа `Short`, `Byte`, `Int`, `Long`, `Float`, `Double`, `Char`, `String`.
- `@EnumSource` — передает Enum-константы.
- `@MethodSource` — передает внешние методы, генерирующие потоки.
- `@CsvSource` — использует значения CSV как источник для параметров.

[К содержанию](#content)

---

#### 7. Тайм-аут теста <a id="тайм-аут-теста"></a>  
В JUnit 5 можно задать тайм-аут для теста с помощью:
```java
Assertions.assertTimeout(Duration.ofMillis(1000), () -> Thread.sleep(10000));
```

[К содержанию](#content)

---

#### 8. Зависимые тесты <a id="зависимые-тесты"></a>
В JUnit зависимые тесты будут помечены как неудачные, если предыдущий тест не пройдет.


[К содержанию](#content)

---

#### 9. Порядок выполнения тестов <a id="порядок-выполнения-тестов"></a>
С JUnit 4 порядок выполнения тестов стал более детерминированным, но всё равно непредсказуемым.
Для контроля порядка выполнения можно использовать аннотацию @FixMethodOrder и указать сортировщик методов.


[К содержанию](#content)

---

#### 10. Пользовательские имена тестов <a id="пользовательские-имена-тестов"></a>
JUnit 5 предоставляет возможность задавать настраиваемые описательные имена для классов и методов тестирования с помощью аннотации @DisplayName.


[К содержанию](#content)

---

### CI, CD <a id="ci-cd"></a>

#### 1. Что такое CI/CD? <a id="что-такое-cicd"></a>  
Непрерывная интеграция (CI) — это процесс автоматической интеграции изменений в основной репозиторий, включая сборку и тестирование кода. Непрерывная доставка (CD) — это автоматический процесс развертывания приложения на продакшн.  
CI фокусируется на сборке и тестировании, а CD — на автоматической доставке приложения.

[К содержанию](#content)

---

#### 2. В чем разница между Continuous Delivery и Continuous Deployment? <a id="разница-cd-cd"></a>  
- **Continuous Delivery (CD)** — процесс автоматического выпуска приложения в продакшн, проходящий все этапы тестирования. Мы можем быть уверены, что приложение высокого качества и готово к релизу.
- **Continuous Deployment (CD)** — это автоматический процесс развертывания одобренного кода в производственную среду.

[К содержанию](#content)

---

#### 3. На больших проектах, реально автоматически всё происходит? <a id="автоматизация-на-больших-проектах"></a>  
На финальной стадии разворачивают девопсы, и хотя автоматизация возможна, важно, чтобы был возможность отката в случае ошибок. Если процесс оставлять на откуп машинам, могут возникнуть серьезные проблемы.

[К содержанию](#content)

---

#### 4. Что такое контейнеризация и виртуализация? <a id="контейнеризация-и-виртуализация"></a>  
- **Контейнеризация** — это технология изоляции приложений и их зависимостей, упакованных в единое исполняемое окружение — контейнер.  
Контейнеры легкие, с нативной производительностью и используют ОС хост-системы.
  
- **Виртуализация** — создание виртуальных машин, которые используют физические ресурсы, такие как сервер, сеть, хранилища и операционные системы. Виртуальные машины тяжелее, с ограниченной производительностью и запускаются с собственной ОС.

[К содержанию](#content)

---

#### 5. CI/CD цикл <a id="ci-cd-цикл"></a>  
CI/CD цикл включает в себя следующие шаги:
1. Написание кода.
2. Сборка.
3. Тестирование.
4. Развертывание.

[К содержанию](#content)

---

#### 6. Практический опыт с CI/CD инструментом <a id="опыт-с-ci-cd-инструментом"></a>  
Для практического опыта с инструментами CI/CD можно использовать:
- **GitLabCI**
- **Appveyor**
- **Travis-CI**
- **Jenkins**
- **GitHub Actions**

[К содержанию](#content)

---

#### 7. Docker (базовые знания) <a id="docker"></a>  
Docker состоит из Docker файлов, которые содержат образы (имаджи) для создания контейнеров. Docker упрощает настройку виртуальных машин и конфигураций, позволяя упаковку приложений со всеми зависимостями в единый контейнер. Это уменьшает объем работы для девопсов.

[К содержанию](#content)

---

#### 8. В чем отличие виртуализации и контейнеризации? <a id="отличие-виртуализации-и-контейнеризации"></a>  
- **Контейнеризация**: 
  - Легковесные.
  - Используют "нативную" ОС.
  - Запуск за секунды.
  - Меньшая изоляция, менее безопасна.
  
- **Виртуализация**:
  - Тяжеловесные.
  - Каждая ВМ запускает свою ОС.
  - Запуск за минуты.
  - Полностью изолирована и более безопасна.

[К содержанию](#content)

---

### Инструменты (фреймворки) автоматизации <a id="инструменты-автоматизации"></a>

#### 1. Основные инструменты тестирования для выбранного языка: Java <a id="инструменты-java"></a>  
В Java наиболее популярными фреймворками для автоматизации тестирования являются **JUnit** и **TestNG**.

[К содержанию](#content)

---

#### 2. Разница между JUnit и TestNG <a id="разница-junit-testng"></a>  
- **JUnit** был основой для TestNG и обеспечивает более простое использование для функциональных тестов.
- **TestNG** предоставляет больше возможностей для параллельного выполнения, параметризации и настройки тестов через XML-конфигурацию.

[К содержанию](#content)

---

#### 3. Основные аннотации <a id="основные-аннотации"></a>  
- `@Test` — объявление тестового метода.
- `@BeforeEach`, `@AfterEach` — выполняются до и после каждого теста.
- `@BeforeAll`, `@AfterAll` — выполняются до и после всех тестов в классе (для JUnit).
- `@DisplayName` — задает описание теста.

[К содержанию](#content)

---

#### 4. Параметризация тестов <a id="параметризация"></a>  
Параметризация позволяет запускать тесты с различными наборами данных. В **JUnit 5** для этого используется аннотация `@ParameterizedTest`.  
**TestNG** поддерживает два способа параметризации:  
- **@DataProvider** — позволяет передавать наборы данных в тестовый метод.
- **@Parameter** — передает параметры из конфигурации.

[К содержанию](#content)

---

#### 5. Выборочный запуск тестов <a id="выборочный-запуск"></a>  
В **TestNG** можно запустить выборочные тесты, указав их в конфигурационном XML-файле. Для этого нужно указать абсолютные пути к тестам и настроить соответствующие теги в XML-файле.

[К содержанию](#content)

---

#### 6. Параллельный запуск в несколько потоков <a id="параллельный-запуск"></a>  
В **TestNG** для параллельного запуска тестов в несколько потоков используется конфигурационный XML-файл, где в теге `suite` указывается параметр `parallel="tests"`.  
Можно указать, сколько потоков будет работать одновременно, например, 2 или 3 потока.

[К содержанию](#content)

---

#### 7. Механизм перезапуска и конфигурационные файлы <a id="механизм-перезапуска-и-конфигурационные-файлы"></a>  
**TestNG** поддерживает механизм перезапуска тестов, который позволяет повторно запускать тесты при необходимости. Конфигурационные файлы **TestNG** обычно имеют формат XML и позволяют точно настраивать выполнение тестов, включая зависимость от других тестов, параллельное выполнение и другие параметры.

[К содержанию](#content)

---

#### 8. Разница в параметризации между TestNG и JUnit <a id="разница-в-параметризации"></a>  
В **TestNG** для параметризации можно использовать `@DataProvider` или `@Parameter`, в то время как в **JUnit** используются аннотации типа `@ValueSource`, `@CsvSource` или `@MethodSource`. Эти аннотации имеют различные способы работы с тестовыми данными.

[К содержанию](#content)

---

#### 9. Механизм перезапуска в TestNG <a id="механизм-перезапуска-в-testng"></a>  
**TestNG** также включает механизм перезапуска тестов, который позволяет автоматически повторно запускать неудачные тесты.

[К содержанию](#content)

### Инструменты для управления проектом (Package Manager) <a id="package-manager"></a>

#### 1. Зачем нужны менеджеры пакетов? <a id="зачем-нужны"></a>  
Менеджеры пакетов используются для управления зависимостями в проектах, автоматизации сборки и упрощения процесса интеграции внешних библиотек.

[К содержанию](#content)

---

#### 2. Популярные менеджеры пакетов <a id="популярные-менеджеры"></a>  
Некоторые известные package manager'ы для разных языков программирования:
- **Java**: Maven, Gradle
- **JavaScript**: npm, Yarn
- **Python**: pip
- **C#**: NuGet
- **Ruby**: Bundler

[К содержанию](#content)

---

#### 3. Основные параметры конфигурации Maven <a id="конфигурация-maven"></a>  
Maven использует XML-файл `pom.xml`, который содержит метаинформацию о проекте.  

- **groupId** – Уникальный идентификатор группы, к которой принадлежит проект.  
  *Пример*: `net.proselyte.video`
- **artifactId** – Уникальный идентификатор проекта в рамках группы.  
  *Пример*: `maven-video`
- **version** – Версия проекта.  
  *Пример*: `1.0.0`
- **dependencies** – Раздел, где указываются зависимости проекта.

[К содержанию](#content)

---

#### 4. Жизненный цикл Maven <a id="жизненный-цикл-maven"></a>  
Maven имеет несколько фаз жизненного цикла:

| Фаза      | Описание |
|-----------|---------|
| `validate` | Проверка корректности метаинформации проекта. |
| `compile` | Компиляция исходных файлов. |
| `test` | Запуск тестов без развертывания проекта. |
| `package` | Упаковка проекта в архив (JAR, WAR и др.). |
| `verify` | Проверка целостности артефакта и соответствия требованиям. |
| `install` | Установка артефакта в локальный репозиторий. |
| `deploy` | Развертывание проекта в удаленном репозитории или на сервере. |

[К содержанию](#content)

---

#### 5. Основные команды Maven <a id="основные-команды-maven"></a>  
- `mvn clean` – очистка временных файлов проекта.
- `mvn build` – сборка проекта.
- `mvn site` – генерация документации по проекту.

[К содержанию](#content)

### Локаторы <a id="локаторы"></a>

#### 1. Что такое локатор? <a id="что-такое-локатор"></a>
Локатор — это механизм поиска элементов на веб-странице, используемый в автоматизированном тестировании.  
С его помощью можно находить, кликать, заполнять формы и проверять содержимое элементов.

[К содержанию](#локаторы)

---

#### 2. Абсолютный и относительный путь <a id="пути"></a>

| Тип пути  | Описание |
|-----------|---------|
| **Абсолютный** (`//`) | Путь, который начинается от корня HTML-документа и указывает точный маршрут к элементу. |
| **Относительный** (`/`) | Путь, который позволяет переходить на нужный элемент, минуя промежуточные узлы. |

**Примеры:**  
- Абсолютный: `//html/body/div[1]/div[2]/span`  
- Относительный: `//div[@class='example']//span`

[К содержанию](#локаторы)

---

#### 3. Разница между CSS и XPath <a id="разница-css-xpath"></a>

| Селектор | Описание |
|-----------|---------|
| **CSS** | Использует классы, идентификаторы и атрибуты для поиска элементов (`.class`, `#id`, `[attribute=value]`). |
| **XPath** | Язык запросов, позволяющий находить элементы по структуре документа (`//div[@class='example']`). |

**Примеры:**  
- CSS: `div.container > p.text`
- XPath: `//div[@class='container']/p[@class='text']`

[К содержанию](#локаторы)

---

#### 4. Основные селекторы XPath <a id="основные-селекторы-xpath"></a>
- `//tag[@attribute='value']` — поиск элемента по атрибуту
- `//div[contains(@class, 'example')]` — поиск элемента с классом, содержащим `example`
- `//input[starts-with(@name, 'user')]` — поиск элементов, где `name` начинается с `user`
- `//button[text()='Купить']` — поиск элемента по тексту
- `//a[last()]` — поиск последнего элемента `a`

[К содержанию](#локаторы)

---

#### 5. XPath-оси <a id="xpath-оси"></a>

| Ось | Описание |
|------|---------|
| `ancestor::` | Возвращает всех предков узла. |
| `ancestor-or-self::` | Возвращает предков и сам элемент. |
| `following-sibling::` | Возвращает соседей ниже по DOM. |
| `preceding-sibling::` | Возвращает соседей выше по DOM. |
| `parent::` | Возвращает родительский элемент. |

**Пример:**  
- `//div[@class='example']/ancestor::body` — найдет `body`, который является предком `div.example`
- `//span[@id='text']/parent::div` — найдет родительский `div` у `span`  

[К содержанию](#локаторы)

---

#### 6. Функции в XPath <a id="xpath-функции"></a>

| Функция | Описание |
|----------|---------|
| `last()` | Возвращает последний элемент. |
| `text()` | Ищет элемент по тексту. |
| `contains()` | Проверяет, содержит ли текст указанное значение. |
| `starts-with()` | Проверяет, начинается ли текст с указанного значения. |

**Примеры:**  
- `//p[contains(text(), 'Привет')]` — найдет `p`, содержащий "Привет"
- `//h1[starts-with(text(), 'Заголовок')]` — найдет `h1`, начинающийся с "Заголовок"

[К содержанию](#локаторы)

---

[Инициализация браузера](#инициализация-браузера)

### Инициализация браузера <a id="инициализация-браузера"></a>

---

### Что такое Selenium WebDriver? <a id="что-такое-selenium-webdriver"></a>

**Selenium WebDriver** — это инструмент для автоматизации браузера, который позволяет программно управлять его поведением. Он имитирует действия пользователя, такие как клик, ввод текста, навигацию по страницам и проверку элементов. WebDriver может работать как локально, так и удаленно.

#### Основные компоненты для работы с WebDriver:
1. **Браузер** — тот, который необходимо автоматизировать.
2. **Драйвер браузера** — мост между Selenium и браузером. Он запускает браузер, отправляет ему команды и управляет его закрытием.
3. **Скрипт (тест)** — код на определенном языке программирования, содержащий команды для WebDriver.

[К содержанию](#инициализация-браузера)

---

### Что такое browser capabilities? <a id="browser-capabilities"></a>

**Browser Capabilities** — это настройки, определяющие, как WebDriver будет запускать и конфигурировать браузер.  
Они позволяют задать:
- Имя браузера (Chrome, Firefox, Edge).
- Версию браузера.
- Операционную систему.
- Разрешение экрана.
- Запуск в **headless**-режиме (без UI).

Используется при настройке Selenium Grid или облачных решений для тестирования.

[К содержанию](#инициализация-браузера)

---

### Что означает headless-режим? <a id="headless-режим"></a>

**Headless-режим** — это запуск браузера без графического интерфейса.  
Он позволяет:
- Выполнять тесты быстрее, так как UI не рендерится.
- Запускать тесты на серверах без GUI.
- Экономить ресурсы.

Чаще всего используется для CI/CD пайплайнов.

[К содержанию](#инициализация-браузера)

---

### Что такое JSON Wire Protocol? <a id="json-wire-protocol"></a>

**JSON Wire Protocol (JSON-WP)** — это протокол, который используется для передачи команд от Selenium WebDriver к браузеру.  
Он основан на REST API и работает по HTTP.

#### Как это работает:
1. WebDriver отправляет HTTP-запрос в формате JSON.
2. Браузерный драйвер (например, chromedriver) получает команду и выполняет её.
3. Драйвер отправляет ответ в формате JSON.

С 2018 года **WebDriver W3C** заменил JSON Wire Protocol, что улучшило стабильность и совместимость.

[К содержанию](#инициализация-браузера)

---

### Как инициализировать Singleton и Abstract Factory? <a id="singleton-abstract-factory"></a>

#### **Singleton**
Шаблон проектирования, который гарантирует, что у класса есть только один экземпляр.  
Используется для управления единственным экземпляром WebDriver во время тестов.

#### **Abstract Factory**
Шаблон, который позволяет создавать семейства связанных объектов без указания их конкретных классов.  
Применяется для инициализации WebDriver с разными настройками в зависимости от браузера.

[К содержанию](#инициализация-браузера)

---

### Что такое browser-менеджер? <a id="browser-менеджер"></a>

**Browser-менеджер** — это инструмент, который автоматически загружает и управляет драйверами браузеров.  
Примеры:
- **WebDriverManager** (автоматически скачивает и обновляет драйверы).
- **Selenium Grid** (распределенное выполнение тестов в разных браузерах).

Использование browser-менеджера снижает необходимость ручной настройки драйверов.

[К содержанию](#инициализация-браузера)

---

[Selenium API](#selenium-api)

### Selenium API <a id="selenium-api"></a>

Selenium — это набор инструментов для автоматизации тестирования веб-приложений. Он состоит из нескольких компонентов:

- **Selenium IDE** (Integrated Development Environment) — плагин для браузера Firefox, который позволяет записывать, сохранять и воспроизводить сценарии тестирования веб-страниц.
- **WebDriver** — инструмент для автоматизации реального браузера с использованием родного API каждого браузера. Поддерживает множество языков программирования, таких как Java, .NET, PHP и другие.
- **Grid** — позволяет синхронно и контролируемо запускать тесты на разных платформах и устройствах, а также эффективно масштабировать тестирование.

[К содержанию](#selenium-api)

---

### Методы класса WebDriver, WebElement, By <a id="методы-webdriver-webelement-by"></a>

#### **WebDriver**
`WebDriver` является самой важной сущностью для автоматизации браузера. Основной поток выполнения теста часто строится вокруг экземпляра этого класса.

#### **WebElement**
`WebElement` представляет собой абстракцию веб-элемента, такого как кнопки, поля ввода и другие элементы. Этот класс инкапсулирует методы взаимодействия с элементами и получения их текущего состояния.

#### **By**
`By` — класс, который содержит статические методы для поиска веб-элементов по различным параметрам: `id`, `name`, `className`, `tagName`, `linkText`, `partialLinkText`, `cssSelector`, `xpath`. Это основной способ идентификации элементов на веб-странице.

[К содержанию](#selenium-api)

---

### Actions <a id="actions"></a>

**Actions** в Selenium WebDriver позволяют имитировать действия пользователя, такие как клики, перетаскивание элементов и другие сложные операции.

- **Клик левой кнопкой мыши**: `builder.click(element);`
- **Двойной клик на элементе**: `builder.doubleClick(element).build().perform();`
- **Отпускание клавиши мыши**: `builder.release()`

[К содержанию](#selenium-api)

---

### Alert <a id="alert"></a>

Selenium WebDriver предоставляет средства для работы с диалоговыми окнами, такими как `alert`, `confirm` и `prompt`.

- Для получения доступа к диалоговому окну используется метод:
  `Alert alert = driver.switchTo().alert();`
- Методы интерфейса `Alert`:
  - `accept()` — нажимает кнопку OK.
  - `dismiss()` — нажимает кнопку Cancel или клавишу Esc.
  - `getText()` — возвращает текст диалогового окна.
  - `sendKeys()` — передает текст в поле ввода диалога.

[К содержанию](#selenium-api)

---

### Iframe <a id="iframe"></a>

**Iframe** (inline frame) — это HTML-элемент, который позволяет встраивать другие документы внутри текущего документа. Для работы с фреймами Selenium предоставляет методы для переключения на них.

- Для переключения на фрейм:
  - `driver.switchTo().frame(0);`
  - `driver.switchTo().frame("frameName");`
  - `driver.switchTo().frame(webElement);`

[К содержанию](#selenium-api)

---

### JSExecutor <a id="jsexecutor"></a>

**JavascriptExecutor** — это интерфейс, который предоставляет возможность выполнения JavaScript-кода внутри браузера. Этот интерфейс реализуется всеми драйверами Selenium, такими как `ChromeDriver`, `FirefoxDriver` и другими.

- Методы:
  - `executeScript()` — выполняет синхронный JavaScript-код.
  - `executeAsyncScript()` — выполняет асинхронный JavaScript-код.

[К содержанию](#selenium-api)

---

### Исключения в Selenium <a id="исключения-в-selenium"></a>

Selenium генерирует различные исключения, которые помогают диагностировать проблемы с автоматизацией тестов.

- **ElementNotVisibleException** — возникает, когда элемент не виден на странице.
- **StaleElementReferenceException** — возникает, если ссылка на элемент больше не действительна.
- **TimeoutException** — выбрасывается, когда команда не завершилась за отведенное время.

#### Исключения группы «Not Found»:
- **NoSuchElementException** — возникает, когда элемент не найден.
- **InvalidSelectorException** — генерируется, если задан неправильный селектор (например, ошибка в синтаксисе XPath).
- **NoSuchFrameException** — выбрасывается при переключении на несуществующий фрейм.
- **NoSuchWindowException** — возникает, если окно с заданным именем не найдено.

[К содержанию](#selenium-api)

---

[API: Сериализация и Десериализация](#api-сериализация-и-десериализация)

### Сериализация и Десериализация <a id="api-сериализация-и-десериализация"></a>

Сериализация и десериализация — это два тесно связанных процесса, которые играют ключевую роль в обмене данными между различными системами.

- **Сериализация** — это процесс преобразования объекта в последовательность байтов, что позволяет сохранить его в файл, передать по сети или сохранить в базе данных. Целью сериализации является возможность восстановить объект в точности таким же, как он был, после его передачи или сохранения.

- **Десериализация** — это процесс восстановления объекта из байтовой последовательности. Этот процесс позволяет преобразовать данные обратно в исходную структуру, готовую к использованию в программе.

[К содержанию](#api-сериализация-и-десериализация)

---

### JSON: Формат обмена данными <a id="json"></a>

**JSON** (JavaScript Object Notation) — это легкий текстовый формат обмена данными, который удобен как для людей, так и для машин. Его структура представлена двумя основными компонентами:

1. **Набор пар ключ-значение**: Каждый элемент данных состоит из ключа и соответствующего ему значения, что обеспечивает высокую гибкость и простоту работы.
2. **Упорядоченный набор значений**: Значения могут быть различными типами, такими как строки, числа, массивы и другие объекты.

JSON широко используется для обмена данными между сервером и клиентом, благодаря своей простоте и читаемости.

[К содержанию](#json)

---

### Jackson и Gson: Сериализация и Десериализация в Java <a id="jackson-и-gson"></a>

#### **Jackson**
**Jackson** — это мощная и гибкая библиотека для работы с JSON в Java. Она позволяет легко конвертировать Java-объекты в JSON и обратно, поддерживает различные типы данных и предоставляет возможность использования кастомных сериализаторов и десериализаторов. Jackson также предлагает высокий уровень производительности, что делает его предпочтительным выбором для масштабных приложений и сервисов.

#### **Gson**
**Gson** — это более легковесная и простая в использовании библиотека. Она идеально подходит для начинающих, поскольку требует меньше настроек и дает возможность легко преобразовывать объекты в JSON и наоборот. Однако с точки зрения производительности Jackson часто будет превосходить Gson, так как использует более оптимизированные методы обработки данных.

Итак, если вам нужно быстрое решение для небольших проектов или тестов, Gson может стать отличным выбором, но если производительность имеет решающее значение, Jackson будет вашим другом.

[К содержанию](#jackson-и-gson)

---

### RestAssured и Retrofit: Библиотеки для API-тестирования <a id="restassured-и-retrofit"></a>

#### **RestAssured**
**RestAssured** — это мощная библиотека для тестирования RESTful API. Она позволяет отправлять запросы и проверять ответы с минимальными усилиями, не требуя создания интерфейсов или сервисных классов. RestAssured часто используется, когда важно быстро проверять API без строгой структуры и без необходимости в сложных абстракциях. Если вам нужно протестировать API с высокой гибкостью, RestAssured — ваш лучший выбор.

#### **Retrofit**
**Retrofit** — это библиотека, предназначенная для работы с HTTP-запросами в Android-приложениях и серверных приложениях. В отличие от RestAssured, Retrofit требует создания интерфейсов, которые описывают сервисы и HTTP-методы. Этот подход предоставляет большую строгость и четкость в архитектуре, что может быть полезно в крупных проектах.

В отличие от RestAssured, где проверка ответов может быть сделана "на лету", Retrofit требует более строгого подхода. Это, конечно, может быть преимуществом, когда вам нужно гарантировать, что взаимодействие с API будет четко структурированным.

В заключение, **RestAssured** — это больше для гибкости и быстроты, а **Retrofit** — для структурированных решений и соблюдения архитектурных паттернов.

[К содержанию](#restassured-и-retrofit)

---

